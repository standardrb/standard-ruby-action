name: Smoke Test
on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  checks: write
  contents: write

jobs:
  pass-standard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: ./
        with:
          workdir: examples/good
